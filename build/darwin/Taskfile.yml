version: '3'

includes:
  common: ../Taskfile.yml

vars:
  # Signing configuration - edit these values for your project
  # SIGN_IDENTITY: "Developer ID Application: Your Company (TEAMID)"
  # KEYCHAIN_PROFILE: "my-notarize-profile"
  # ENTITLEMENTS: "build/darwin/entitlements.plist"

  # Docker image for cross-compilation (used when building on non-macOS).
  # Renamed from the wails3-scaffold `wails-cross` so multiple wails3
  # projects on one machine don't collide on the same Docker image tag.
  CROSS_IMAGE: lthn-cross

  # Path to the self-contained lthn-mlx helper binary. Produced by the
  # top-level `task pre-build` (build:lthn-mlx) into build/darwin/bin, then
  # copied into lthn.app/Contents/MacOS alongside the main executable. The
  # binary embeds its own metallib (-tags embed_metallib), so no sibling
  # resource or nested .app is needed — it runs from any path. Override
  # with LTHN_MLX_BIN=/path/to/lthn-mlx for CI / a prebuilt binary.
  LTHN_MLX_BIN: '{{.LTHN_MLX_BIN | default "build/darwin/bin/lthn-mlx"}}'

tasks:
  build:
    summary: Builds the application
    cmds:
      - task: '{{if eq OS "darwin"}}build:native{{else}}build:docker{{end}}'
        vars:
          ARCH: '{{.ARCH}}'
          DEV: '{{.DEV}}'
          OUTPUT: '{{.OUTPUT}}'
          EXTRA_TAGS: '{{.EXTRA_TAGS}}'
    vars:
      DEFAULT_OUTPUT: '{{.BIN_DIR}}/{{.APP_NAME}}'
      OUTPUT: '{{ .OUTPUT | default .DEFAULT_OUTPUT }}'

  build:native:
    summary: Builds the application natively on macOS (Lethean go/ layout)
    internal: true
    deps:
      - task: common:go:work:sync
      - task: common:build:frontend
        vars:
          BUILD_FLAGS:
            ref: .BUILD_FLAGS
          DEV:
            ref: .DEV
      - task: common:generate:icons
    cmds:
      - cd "{{.GO_DIR}}" && go build {{.BUILD_FLAGS}} -ldflags "-X dappco.re/go/update.Version={{.LTHN_VERSION}}" -o "../{{.OUTPUT}}" "{{.GO_CMD}}"
    vars:
      # Dev: optional extra tags; Prod: Wails production tag plus -trimpath.
      # Strip flags (-ldflags="-w -s") removed — they conflict with
      # DuckDB Arrow C symbols that go-store imports via cgo. The
      # -ldflags '-X dappco.re/go/update.Version=…' stamp on the cmd
      # below ships the build version into the update service so it
      # has a real reference to compare against the latest release.
      BUILD_FLAGS: '{{if eq .DEV "true"}}{{if .EXTRA_TAGS}}-tags {{.EXTRA_TAGS}} {{end}}-buildvcs=false -gcflags=all="-l"{{else}}-tags production{{if .EXTRA_TAGS}},{{.EXTRA_TAGS}}{{end}} -trimpath -buildvcs=false{{end}}'
      LTHN_VERSION:
        sh: git describe --tags --always --dirty 2>/dev/null || echo dev
      DEFAULT_OUTPUT: '{{.BIN_DIR}}/{{.APP_NAME}}'
      OUTPUT: '{{ .OUTPUT | default .DEFAULT_OUTPUT }}'
    env:
      GOOS: darwin
      CGO_ENABLED: 1
      GOARCH: '{{.ARCH | default ARCH}}'
      # Note: NO -mmacosx-version-min pin. Any explicit value
      # (12.0 from the scaffold, or 26.0) breaks DuckDB Arrow C
      # symbol linking via go-store's cgo. The system default
      # matches the precompiled DuckDB library and links cleanly.
      # macOS 26.0 is our effective floor anyway (Metal mainline,
      # go-mlx needs it) — let the SDK default carry that.

  build:docker:
    summary: Cross-compiles for macOS using Docker (for Linux/Windows hosts)
    internal: true
    deps:
      - task: common:build:frontend
      - task: common:generate:icons
    preconditions:
      - sh: docker info > /dev/null 2>&1
        msg: "Docker is required for cross-compilation. Please install Docker."
      - sh: docker image inspect {{.CROSS_IMAGE}} > /dev/null 2>&1
        msg: |
          Docker image '{{.CROSS_IMAGE}}' not found.
          Build it first: wails3 task setup:docker
    cmds:
      - docker run --rm -v "{{.ROOT_DIR}}:/app" {{.GO_CACHE_MOUNT}} {{.REPLACE_MOUNTS}} -e APP_NAME="{{.APP_NAME}}" {{if .EXTRA_TAGS}}-e EXTRA_TAGS="{{.EXTRA_TAGS}}"{{end}} {{.CROSS_IMAGE}} darwin {{.DOCKER_ARCH}}
      - docker run --rm -v "{{.ROOT_DIR}}:/app" alpine chown -R $(id -u):$(id -g) /app/bin
      - mkdir -p {{.BIN_DIR}}
      - mv "bin/{{.APP_NAME}}-darwin-{{.DOCKER_ARCH}}" "{{.OUTPUT}}"
    vars:
      DOCKER_ARCH: '{{if eq .ARCH "arm64"}}arm64{{else if eq .ARCH "amd64"}}amd64{{else}}arm64{{end}}'
      DEFAULT_OUTPUT: '{{.BIN_DIR}}/{{.APP_NAME}}'
      OUTPUT: '{{ .OUTPUT | default .DEFAULT_OUTPUT }}'
      # Mount Go module cache for faster builds
      GO_CACHE_MOUNT:
        sh: 'echo "-v ${GOPATH:-$HOME/go}/pkg/mod:/go/pkg/mod"'
      # Extract replace directives from go.mod and create -v mounts for each
      # Handles both relative (=> ../) and absolute (=> /) paths
      REPLACE_MOUNTS:
        sh: |
          grep -E '^replace .* => ' go.mod 2>/dev/null | while read -r line; do
            path=$(echo "$line" | sed -E 's/^replace .* => //' | tr -d '\r')
            # Convert relative paths to absolute
            if [ "${path#/}" = "$path" ]; then
              path="$(cd "$(dirname "$path")" 2>/dev/null && pwd)/$(basename "$path")"
            fi
            # Only mount if directory exists
            if [ -d "$path" ]; then
              echo "-v $path:$path:ro"
            fi
          done | tr '\n' ' '

  build:universal:
    summary: Builds darwin universal binary (arm64 + amd64)
    deps:
      - task: build
        vars:
          ARCH: amd64
          OUTPUT: "{{.BIN_DIR}}/{{.APP_NAME}}-amd64"
      - task: build
        vars:
          ARCH: arm64
          OUTPUT: "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"
    cmds:
      - task: '{{if eq OS "darwin"}}build:universal:lipo:native{{else}}build:universal:lipo:go{{end}}'

  build:universal:lipo:native:
    summary: Creates universal binary using native lipo (macOS)
    internal: true
    cmds:
      - lipo -create -output "{{.BIN_DIR}}/{{.APP_NAME}}" "{{.BIN_DIR}}/{{.APP_NAME}}-amd64" "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"
      - rm "{{.BIN_DIR}}/{{.APP_NAME}}-amd64" "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"

  build:universal:lipo:go:
    summary: Creates universal binary using wails3 tool lipo (Linux/Windows)
    internal: true
    cmds:
      - wails3 tool lipo -output "{{.BIN_DIR}}/{{.APP_NAME}}" -input "{{.BIN_DIR}}/{{.APP_NAME}}-amd64" -input "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"
      - rm -f "{{.BIN_DIR}}/{{.APP_NAME}}-amd64" "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"

  package:
    summary: Packages the application into a `.app` bundle
    deps:
      - task: build
        vars:
          ARCH: '{{.ARCH | default ARCH}}'
    cmds:
      - task: create:app:bundle

  package:universal:
    summary: Packages darwin universal binary (arm64 + amd64)
    deps:
      - task: build:universal
    cmds:
      - task: create:app:bundle

  create:dmg:
    summary: Creates a DMG disk image for distribution
    # Mirrors wails3's internal dmg.Creator wiring at
    # v3/internal/commands/dmg/dmg.go — alpha.91 ships the Go
    # package but tool_package.go has the dispatch disabled with
    # a stale "missing dmg package" error. macos.mdx documents
    # the `darwin:create:dmg` task as the canonical entrypoint;
    # we provide the hdiutil flow locally until upstream re-enables.
    #
    # The staged temp directory + Applications symlink gives the
    # standard drag-to-install UX: the mounted volume shows the
    # .app on the left and an Applications shortcut on the right.
    #
    # Consumes whatever .app already exists at bin/<APP_NAME>.app —
    # so the caller chooses whether that's the host-arch .app from
    # `wails3 task darwin:package` or the universal .app from
    # `wails3 task darwin:package:universal`. Run one of those first.
    preconditions:
      - sh: test -d "{{.BIN_DIR}}/{{.APP_NAME}}.app"
        msg: "{{.BIN_DIR}}/{{.APP_NAME}}.app not found. Run `wails3 task darwin:package` (host arch) or `wails3 task darwin:package:universal` (both arches) first."
    cmds:
      - |-
        set -e
        STAGE="$(mktemp -d -t lthn-dmg-stage)"
        trap 'rm -rf "$STAGE"' EXIT
        cp -R "{{.BIN_DIR}}/{{.APP_NAME}}.app" "$STAGE/"
        ln -s /Applications "$STAGE/Applications"
        rm -f "{{.BIN_DIR}}/{{.APP_NAME}}.dmg"
        hdiutil create \
          -volname "{{.APP_NAME}}" \
          -srcfolder "$STAGE" \
          -format UDZO -ov \
          "{{.BIN_DIR}}/{{.APP_NAME}}.dmg"

  create:app:bundle:
    summary: Creates an `.app` bundle
    cmds:
      - mkdir -p "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/MacOS"
      - mkdir -p "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/Resources"
      # Mirror the icon-asset state from build/ — see darwin:run for the
      # same pattern. A stale Assets.car in the prod .app overrides
      # icons.icns and surfaces the wrong icon in Dock / Launchpad.
      - rm -f "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/Resources/Assets.car"
      - cp build/darwin/icons.icns "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/Resources"
      # Assets.car is intentionally not generated — see build/Taskfile.yml
      # `generate:icons` for why. The if-block stays so a future re-enable
      # of the Icon Composer path doesn't need this site to change.
      - |
        if [ -f build/darwin/Assets.car ]; then
          cp build/darwin/Assets.car "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/Resources"
        fi
      - cp "{{.BIN_DIR}}/{{.APP_NAME}}" "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/MacOS"
      - cp build/darwin/Info.plist "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents"
      # Self-contained helper: lthn-mlx (embeds its own metallib via
      # -tags embed_metallib) dropped alongside the main executable in
      # Contents/MacOS, so the lthn binary can spawn it via pkg/lemma with
      # no metallib resolution and no nested .app. Produced by `task
      # pre-build`; codesign:adhoc below signs it with the rest of the .app.
      - |-
        if [ -x "{{.LTHN_MLX_BIN}}" ]; then
          cp "{{.LTHN_MLX_BIN}}" "{{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/MacOS/lthn-mlx"
          echo "  bundled self-contained lthn-mlx from {{.LTHN_MLX_BIN}}"
        else
          echo "  lthn-mlx not found at {{.LTHN_MLX_BIN}} — desktop will need lthn-mlx on PATH at runtime"
          echo "  to stage it: run 'task pre-build' (needs the go-mlx metallib built)"
        fi
      - task: '{{if eq OS "darwin"}}codesign:adhoc{{else}}codesign:skip{{end}}'

  codesign:adhoc:
    summary: Ad-hoc signs the app bundle (macOS only)
    internal: true
    cmds:
      - codesign --force --deep --sign - "{{.BIN_DIR}}/{{.APP_NAME}}.app"

  codesign:skip:
    summary: Skips codesigning when cross-compiling
    internal: true
    cmds:
      - 'echo "Skipping codesign (not available on {{OS}}). Sign the .app on macOS before distribution."'

  run:
    cmds:
      - mkdir -p "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/MacOS"
      - mkdir -p "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/Resources"
      # Mirror the icon-asset state from build/ — stale Assets.car
      # leftovers from a previous Icon Composer cycle would otherwise
      # outrank icons.icns and put the wrong icon in the Dock. If
      # build/darwin/Assets.car comes back in a future cycle, the
      # cp-block below picks it up; the unconditional rm here keeps
      # both directions truthful.
      - rm -f "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/Resources/Assets.car"
      - cp build/darwin/icons.icns "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/Resources"
      - |
        if [ -f build/darwin/Assets.car ]; then
          cp build/darwin/Assets.car "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/Resources"
        fi
      - cp "{{.BIN_DIR}}/{{.APP_NAME}}" "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/MacOS"
      - cp "build/darwin/Info.dev.plist" "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/Info.plist"
      - codesign --force --deep --sign - "{{.BIN_DIR}}/{{.APP_NAME}}.dev.app"
      - '"{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/MacOS/{{.APP_NAME}}"'

  sign:
    summary: Signs the application bundle with Developer ID
    desc: |
      Signs the .app bundle for distribution.
      Configure SIGN_IDENTITY in the vars section at the top of this file.
    deps:
      - task: package
    cmds:
      - wails3 tool sign --input "{{.BIN_DIR}}/{{.APP_NAME}}.app" --identity "{{.SIGN_IDENTITY}}" {{if .ENTITLEMENTS}}--entitlements {{.ENTITLEMENTS}}{{end}}
    preconditions:
      - sh: '[ -n "{{.SIGN_IDENTITY}}" ]'
        msg: "SIGN_IDENTITY is required. Set it in the vars section at the top of build/darwin/Taskfile.yml"

  sign:notarize:
    summary: Signs and notarizes the application bundle
    desc: |
      Signs the .app bundle and submits it for notarization.
      Configure SIGN_IDENTITY and KEYCHAIN_PROFILE in the vars section at the top of this file.

      Setup (one-time):
        wails3 signing credentials --apple-id "you@email.com" --team-id "TEAMID" --password "app-specific-password" --profile "my-profile"
    deps:
      - task: package
    cmds:
      - wails3 tool sign --input "{{.BIN_DIR}}/{{.APP_NAME}}.app" --identity "{{.SIGN_IDENTITY}}" {{if .ENTITLEMENTS}}--entitlements {{.ENTITLEMENTS}}{{end}} --notarize --keychain-profile {{.KEYCHAIN_PROFILE}}
    preconditions:
      - sh: '[ -n "{{.SIGN_IDENTITY}}" ]'
        msg: "SIGN_IDENTITY is required. Set it in the vars section at the top of build/darwin/Taskfile.yml"
      - sh: '[ -n "{{.KEYCHAIN_PROFILE}}" ]'
        msg: "KEYCHAIN_PROFILE is required. Set it in the vars section at the top of build/darwin/Taskfile.yml"
